@page "/"

@inject IHttpContextAccessor Accessor

@inject Settings Settings

@code {

    protected override void OnInitialized()
    {
        Accessor.HttpContext.Response.Redirect(Settings.EnableShop ? "/user/select" : "/upload");
    }
}