@page "/"

@inject IHttpContextAccessor Accessor

@inject Settings Settings

@code {

    protected override void OnInitialized()
    {
        //Accessor.HttpContext.Response.Redirect(Settings.UsersEnabled ? "/user/select" : "/browse");
        Accessor.HttpContext.Response.Redirect("/browse");
    }
}